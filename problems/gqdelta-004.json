{
  "id": "gqdelta-004",
  "title": "Frames to seconds",
  "concept": "gq-delta",
  "difficulty": 0,
  "prompt": "If every frame lasts delta seconds, return how many seconds a number of frames lasts.",
  "signature": "func solve(frames: int, delta: float) -> float:",
  "starter": "func solve(frames: int, delta: float) -> float:\n\treturn 0.0",
  "tests": [
    {
      "args": [
        50,
        0.02
      ],
      "expect": 1
    },
    {
      "args": [
        25,
        0.02
      ],
      "expect": 0.5
    },
    {
      "args": [
        0,
        0.016
      ],
      "expect": 0
    }
  ],
  "hint": "Count times length of each.",
  "solution": "func solve(frames: int, delta: float) -> float:\n\treturn frames * delta"
}
