{
  "id": "gqread-011",
  "title": "Name the frames per second",
  "concept": "gq-readable",
  "difficulty": 0,
  "prompt": "This turns frames into seconds at 60 frames per second. Name the 60.0 `FRAMES_PER_SECOND`.",
  "signature": "func frames_to_seconds(frames) -> float:",
  "fn": "frames_to_seconds",
  "starter": "func frames_to_seconds(frames) -> float:\n\treturn frames / 60.0",
  "require_names": [
    "FRAMES_PER_SECOND"
  ],
  "once_only": [
    60
  ],
  "tests": [
    {
      "name": "60 frames is 1 second",
      "args": [
        60
      ],
      "expect": 1
    },
    {
      "name": "30 frames is half a second",
      "args": [
        30
      ],
      "expect": 0.5
    },
    {
      "name": "0 frames is 0",
      "args": [
        0
      ],
      "expect": 0
    }
  ],
  "hints": [
    "Keep the .0 in the constant, or the division drops the decimals.",
    "`const FRAMES_PER_SECOND = 60.0`"
  ],
  "docs": [
    {
      "name": "const NAME = value",
      "what": "Declares a named value that never changes. Written in CAPITALS by convention."
    }
  ],
  "solution": "func frames_to_seconds(frames) -> float:\n\tconst FRAMES_PER_SECOND = 60.0\n\treturn frames / FRAMES_PER_SECOND"
}
