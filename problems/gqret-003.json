{
  "id": "gqret-003",
  "title": "Return an expression",
  "concept": "gq-return",
  "difficulty": 0,
  "prompt": "Write `double`, which returns its parameter times 2. solve returns double(n).",
  "signature": "func solve(n: int) -> int:",
  "starter": "func solve(n: int) -> int:\n\treturn n",
  "require_methods": [
    "double"
  ],
  "tests": [
    {
      "args": [
        4
      ],
      "expect": 8
    },
    {
      "args": [
        0
      ],
      "expect": 0
    },
    {
      "args": [
        -3
      ],
      "expect": -6
    }
  ],
  "hint": "return can be followed by a calculation, not just a name.",
  "solution": "func solve(n: int) -> int:\n\treturn double(n)\n\nfunc double(value: int) -> int:\n\treturn value * 2"
}
