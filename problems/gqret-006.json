{
  "id": "gqret-006",
  "title": "Code after return never runs",
  "concept": "gq-return",
  "difficulty": 1,
  "prompt": "Run the starter as it is: \"after\" is never printed, because return ends the function at once. Fix the helper so it prints both words and still returns 1.",
  "signature": "func after_fix():",
  "fn": "after_fix",
  "starter": "func after_fix():\n\treturn early()\n\nfunc early():\n\tprint(\"before\")\n\treturn 1\n\tprint(\"after\")",
  "tests": [
    {
      "name": "Both words are printed and 1 comes back",
      "args": [],
      "expect": 1,
      "out": [
        "before",
        "after"
      ]
    }
  ],
  "hints": [
    "Anything that must happen has to come before the return line.",
    "Move `print(\"after\")` above `return 1`."
  ],
  "docs": [
    {
      "name": "return value",
      "what": "Ends the function and hands value back to whoever called it."
    },
    {
      "name": "print(value)",
      "what": "Shows value in the output. Takes anything; returns nothing."
    }
  ],
  "solution": "func after_fix():\n\treturn early()\n\nfunc early():\n\tprint(\"before\")\n\tprint(\"after\")\n\treturn 1",
  "require_methods": [
    "early"
  ]
}
