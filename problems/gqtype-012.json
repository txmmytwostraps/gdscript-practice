{
  "id": "gqtype-012",
  "title": "Convert to satisfy the hint",
  "concept": "gq-types",
  "difficulty": 1,
  "prompt": "A health number becomes a label with \"hp\" on the end, through a `to_label` helper that label_for uses. label_for(80) is \"80hp\".",
  "signature": "func label_for(n: int) -> String:",
  "fn": "label_for",
  "starter": "func label_for(n: int) -> String:\n\treturn \"hp\"",
  "tests": [
    {
      "name": "80 gives 80hp",
      "args": [
        80
      ],
      "expect": "80hp"
    },
    {
      "name": "0 gives 0hp",
      "args": [
        0
      ],
      "expect": "0hp"
    }
  ],
  "hints": [
    "to_label takes an int and returns a String; the return hint means the number must be turned into text first. label_for returns to_label(n).",
    "`return str(value) + \"hp\"`"
  ],
  "docs": [
    {
      "name": "func f() -> int:",
      "what": "Return type hint: the function promises to return that type."
    },
    {
      "name": "str(value)",
      "what": "Returns the value as text."
    }
  ],
  "solution": "func label_for(n: int) -> String:\n\treturn to_label(n)\n\nfunc to_label(value: int) -> String:\n\treturn str(value) + \"hp\"",
  "allow_type_hints": true,
  "require_methods": [
    "to_label"
  ],
  "generator": [
    {
      "int": [
        0,
        120
      ]
    }
  ]
}
