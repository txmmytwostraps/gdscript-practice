{
  "id": "gqtype-015",
  "title": "Read a hinted signature",
  "concept": "gq-types",
  "difficulty": 0,
  "prompt": "This helper is already written with full hints. Call it from `solve` with 3 and 4. Then return what it gives you.",
  "signature": "func solve() -> int:",
  "starter": "func solve() -> int:\n\treturn 0\n\nfunc add(a: int, b: int) -> int:\n\treturn a + b",
  "tests": [
    {
      "name": "3 plus 4 is 7",
      "args": [],
      "expect": 7
    }
  ],
  "hints": [
    "The hints tell you it takes two ints and gives back an int.",
    "`return add(3, 4)`"
  ],
  "docs": [
    {
      "name": "func f(a: int, b: float):",
      "what": "Parameter type hints: Godot checks what is passed in."
    },
    {
      "name": "func f() -> int:",
      "what": "Return type hint: the function promises to return that type."
    }
  ],
  "solution": "func solve() -> int:\n\treturn add(3, 4)\n\nfunc add(a: int, b: int) -> int:\n\treturn a + b",
  "allow_type_hints": true
}
