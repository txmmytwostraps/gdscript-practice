{
  "id": "gqret-005",
  "title": "Print, then return",
  "concept": "gq-return",
  "difficulty": 0,
  "prompt": "Write `add`, which prints \"adding\" and then returns its two parameters added together. `solve` returns add(a, b). Both the printed line and the returned value are checked.",
  "signature": "func solve(a, b):",
  "starter": "func solve(a, b):\n\treturn a + b",
  "tests": [
    {
      "name": "adding is printed and the sum comes back",
      "args": [
        2,
        3
      ],
      "expect": 5,
      "out": [
        "adding"
      ]
    },
    {
      "name": "Zeros still print adding and return 0",
      "args": [
        0,
        0
      ],
      "expect": 0,
      "out": [
        "adding"
      ]
    }
  ],
  "hints": [
    "A function can do things before it returns; returning is the last thing.",
    "`print(\"adding\")` then `return x + y` inside add."
  ],
  "docs": [
    {
      "name": "return value",
      "what": "Ends the function and hands value back to whoever called it."
    },
    {
      "name": "print(value)",
      "what": "Shows value in the output. Takes anything; returns nothing."
    }
  ],
  "solution": "func solve(a, b):\n\treturn add(a, b)\n\nfunc add(x, y):\n\tprint(\"adding\")\n\treturn x + y",
  "require_methods": [
    "add"
  ]
}
