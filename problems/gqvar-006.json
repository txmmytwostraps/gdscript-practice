{
  "id": "gqvar-006",
  "title": "Update with +=",
  "concept": "gq-variables",
  "difficulty": 0,
  "prompt": "The variable `coins` starts with the value of n. Add 5 to it using +=. Return coins.",
  "signature": "func solve(n: int) -> int:",
  "starter": "func solve(n: int) -> int:\n\tvar coins = n\n\treturn coins",
  "tests": [
    {
      "args": [
        0
      ],
      "expect": 5
    },
    {
      "args": [
        10
      ],
      "expect": 15
    },
    {
      "args": [
        -5
      ],
      "expect": 0
    }
  ],
  "hint": "`coins += 5` means the same as `coins = coins + 5`.",
  "solution": "func solve(n: int) -> int:\n\tvar coins = n\n\tcoins += 5\n\treturn coins"
}
