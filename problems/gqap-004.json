{
  "id": "gqap-004",
  "title": "What is left",
  "concept": "gq-appendpop",
  "difficulty": 0,
  "prompt": "Serve the first order with pop_front, then return the queue that remains.",
  "signature": "func solve(queue):",
  "starter": "func solve(queue):\n\treturn queue",
  "tests": [
    {
      "name": "After serving soup, pie remains",
      "args": [
        [
          "soup",
          "pie"
        ]
      ],
      "expect": [
        "pie"
      ]
    },
    {
      "name": "Serving the only order leaves nothing",
      "args": [
        [
          "tea"
        ]
      ],
      "expect": []
    }
  ],
  "hints": [
    "pop_front changes the array; ignore what it returns this time.",
    "`queue.pop_front()` on its own line, then `return queue`."
  ],
  "docs": [
    {
      "name": "list.pop_front()",
      "what": "Removes the first item and returns it."
    }
  ],
  "solution": "func solve(queue):\n\tqueue.pop_front()\n\treturn queue",
  "generator": [
    {
      "array": {
        "len": [
          1,
          4
        ],
        "of": {
          "pick": [
            "soup",
            "pie",
            "tea"
          ]
        }
      }
    }
  ]
}
