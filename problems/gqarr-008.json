{
  "id": "gqarr-008",
  "title": "Empty or not",
  "concept": "gq-arrays",
  "difficulty": 0,
  "prompt": "Return true if the robot's inventory has no items.",
  "signature": "func is_empty(items):",
  "fn": "is_empty",
  "starter": "func is_empty(items):\n\treturn false",
  "tests": [
    {
      "name": "Empty is true",
      "args": [
        []
      ],
      "expect": true
    },
    {
      "name": "One item is false",
      "args": [
        [
          1
        ]
      ],
      "expect": false
    },
    {
      "name": "Two zeros is false",
      "args": [
        [
          0,
          0
        ]
      ],
      "expect": false
    }
  ],
  "hints": [
    "Compare the size with zero, or use the method that asks the question.",
    "`return items.is_empty()`"
  ],
  "docs": [
    {
      "name": "list.is_empty()",
      "what": "Returns true when the array has no items."
    },
    {
      "name": "list.size()",
      "what": "Returns how many items the array holds."
    }
  ],
  "solution": "func is_empty(items):\n\treturn items.is_empty()",
  "generator": [
    {
      "array": {
        "len": [
          0,
          4
        ],
        "of": {
          "int": [
            0,
            3
          ]
        }
      }
    }
  ]
}
