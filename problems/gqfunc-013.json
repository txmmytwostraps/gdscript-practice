{
  "id": "gqfunc-013",
  "title": "The missing call",
  "concept": "gq-functions",
  "difficulty": 0,
  "prompt": "The robot has a `charge` function that prints \"Charging\", but it never runs because nothing calls it. Make `solve` call it, so the output is \"Charging\".",
  "signature": "func solve():",
  "starter": "func solve():\n\tpass\n\nfunc charge():\n\tprint(\"Charging\")",
  "require_methods": [
    "charge"
  ],
  "tests": [
    {
      "name": "Charging is printed",
      "args": [],
      "expect": null,
      "out": [
        "Charging"
      ]
    }
  ],
  "hints": [
    "Defining a function does not run it. Something has to call it.",
    "Replace pass in solve with `charge()`."
  ],
  "docs": [
    {
      "name": "print(value)",
      "what": "Shows value in the output. Takes anything; returns nothing."
    }
  ],
  "solution": "func solve():\n\tcharge()\n\nfunc charge():\n\tprint(\"Charging\")"
}
