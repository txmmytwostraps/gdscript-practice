{
  "id": "gqap-010",
  "title": "Cancel an order",
  "concept": "gq-appendpop",
  "difficulty": 1,
  "prompt": "A customer cancels an order. cancel_order gives back the queue without the first matching order; if it is not there, nothing changes.",
  "signature": "func cancel_order(queue, order):",
  "fn": "cancel_order",
  "starter": "func cancel_order(queue, order):\n\treturn queue",
  "tests": [
    {
      "name": "pie removed",
      "args": [
        [
          "soup",
          "pie",
          "tea"
        ],
        "pie"
      ],
      "expect": [
        "soup",
        "tea"
      ]
    },
    {
      "name": "Nothing to cancel",
      "args": [
        [
          "soup"
        ],
        "cake"
      ],
      "expect": [
        "soup"
      ]
    }
  ],
  "hints": [
    "erase removes by value: one call on the queue, then give the queue back.",
    "`queue.erase(order)`"
  ],
  "docs": [
    {
      "name": "list.erase(value)",
      "what": "Removes the first copy of value. Returns nothing."
    }
  ],
  "solution": "func cancel_order(queue, order):\n\tqueue.erase(order)\n\treturn queue",
  "generator": [
    {
      "array": {
        "len": [
          1,
          5
        ],
        "of": {
          "pick": [
            "soup",
            "pie",
            "tea"
          ]
        }
      }
    },
    {
      "pick": [
        "pie",
        "cake"
      ]
    }
  ]
}
