{
  "id": "gqparam-005",
  "title": "A default value",
  "concept": "gq-parameters",
  "difficulty": 1,
  "prompt": "Write `show_score`, which takes a score and a bonus, where bonus has a default of 0, and prints their sum. In `report_scores`, call it once with just the score and once with the score and a bonus of 10, so report_scores(5) prints 5 then 15.",
  "signature": "func report_scores(score):",
  "fn": "report_scores",
  "starter": "func report_scores(score):\n\tpass",
  "require_methods": [
    "show_score"
  ],
  "tests": [
    {
      "name": "5 then 15",
      "args": [
        5
      ],
      "expect": null,
      "out": [
        "5",
        "15"
      ]
    },
    {
      "name": "0 then 10",
      "args": [
        0
      ],
      "expect": null,
      "out": [
        "0",
        "10"
      ]
    }
  ],
  "hints": [
    "A default is written in the definition after an = sign; the caller may leave that parameter out.",
    "`func show_score(score, bonus = 0):` printing `score + bonus`.",
    "First call `show_score(score)`, then `show_score(score, 10)`."
  ],
  "docs": [
    {
      "name": "print(value)",
      "what": "Shows value in the output. Takes anything; returns nothing."
    }
  ],
  "solution": "func report_scores(score):\n\tshow_score(score)\n\tshow_score(score, 10)\n\nfunc show_score(score, bonus = 0):\n\tprint(score + bonus)"
}
