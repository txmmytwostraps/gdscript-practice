{
  "id": "gqloop-002",
  "title": "Add up the time",
  "concept": "gq-loop",
  "difficulty": 1,
  "prompt": "A member `time` starts at 0.0. In `_process`, add delta to it every frame. After 60 frames at 60 per second, solve should return about 1.0 (rounded to whole seconds).",
  "signature": "func solve():",
  "starter": "var time = 0.0\n\nfunc solve():\n\treturn round(time)",
  "tests": [
    {
      "name": "60 frames is 1 second",
      "args": [],
      "expect": 1,
      "frames": 60
    },
    {
      "name": "120 frames is 2 seconds",
      "args": [],
      "expect": 2,
      "frames": 120
    },
    {
      "name": "0 frames is 0",
      "args": [],
      "expect": 0,
      "frames": 0
    }
  ],
  "hints": [
    "delta is the length of one frame in seconds; adding it up gives total time.",
    "`time += delta` inside _process."
  ],
  "docs": [
    {
      "name": "func _process(delta):",
      "what": "Godot calls this once per frame, about 60 times a second. delta is the frame time in seconds."
    },
    {
      "name": "var name = value (outside any function)",
      "what": "A member variable keeps its value from one frame to the next."
    },
    {
      "name": "round(x)",
      "what": "Returns the nearest whole number as a float."
    }
  ],
  "solution": "var time = 0.0\n\nfunc _process(delta):\n\ttime += delta\n\nfunc solve():\n\treturn round(time)",
  "require_methods": [
    "_process"
  ]
}
