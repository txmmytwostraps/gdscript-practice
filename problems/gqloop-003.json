{
  "id": "gqloop-003",
  "title": "Move every frame",
  "concept": "gq-loop",
  "difficulty": 1,
  "prompt": "The robot drives at 120 pixels per second, starting from `x` at 0.0. After 60 frames at 60 per second, x_after_second reports 120.",
  "signature": "func x_after_second():",
  "fn": "x_after_second",
  "starter": "var x = 0.0\n\nfunc x_after_second():\n\treturn round(x)",
  "tests": [
    {
      "name": "One second moves 120",
      "args": [],
      "expect": 120,
      "frames": 60
    },
    {
      "name": "Half a second moves 60",
      "args": [],
      "expect": 60,
      "frames": 30
    },
    {
      "name": "No frames, no movement",
      "args": [],
      "expect": 0,
      "frames": 0
    }
  ],
  "hints": [
    "Speed times delta is the distance for one frame; add it to x in _process.",
    "`x += 120 * delta` inside _process."
  ],
  "docs": [
    {
      "name": "func _process(delta):",
      "what": "Godot calls this once per frame, about 60 times a second. delta is the frame time in seconds."
    },
    {
      "name": "var name = value (outside any function)",
      "what": "A member variable keeps its value from one frame to the next."
    },
    {
      "name": "round(x)",
      "what": "Returns the nearest whole number as a float."
    }
  ],
  "solution": "var x = 0.0\n\nfunc _process(delta):\n\tx += 120 * delta\n\nfunc x_after_second():\n\treturn round(x)",
  "require_methods": [
    "_process"
  ]
}
