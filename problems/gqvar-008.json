{
  "id": "gqvar-008",
  "title": "A variable times itself",
  "concept": "gq-variables",
  "difficulty": 0,
  "prompt": "Store n in a variable called `side`. Then make a second variable `area` equal to side times side. Return area.",
  "signature": "func solve(n: int) -> int:",
  "starter": "func solve(n: int) -> int:\n\treturn 0",
  "require_names": [
    "side",
    "area"
  ],
  "tests": [
    {
      "args": [
        3
      ],
      "expect": 9
    },
    {
      "args": [
        0
      ],
      "expect": 0
    },
    {
      "args": [
        10
      ],
      "expect": 100
    }
  ],
  "hint": "A new variable can be calculated from one you already made. Two var lines, then return the second.",
  "solution": "func solve(n: int) -> int:\n\tvar side = n\n\tvar area = side * side\n\treturn area"
}
