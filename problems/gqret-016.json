{
  "id": "gqret-016",
  "title": "Return with a default",
  "concept": "gq-return",
  "difficulty": 1,
  "prompt": "Write `scaled`, which returns n times factor, where factor has a default of 3. solve returns scaled(n) plus scaled(n, 1).",
  "signature": "func solve(n: int) -> int:",
  "starter": "func solve(n: int) -> int:\n\treturn n",
  "require_methods": [
    "scaled"
  ],
  "tests": [
    {
      "args": [
        2
      ],
      "expect": 8
    },
    {
      "args": [
        0
      ],
      "expect": 0
    },
    {
      "args": [
        5
      ],
      "expect": 20
    }
  ],
  "hint": "Called with one value the helper uses factor 3; called with two it uses what you pass.",
  "solution": "func solve(n: int) -> int:\n\treturn scaled(n) + scaled(n, 1)\n\nfunc scaled(value: int, factor: int = 3) -> int:\n\treturn value * factor"
}
