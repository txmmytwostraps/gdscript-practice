{
  "id": "gqread-006",
  "title": "Name the tile size",
  "concept": "gq-readable",
  "difficulty": 0,
  "prompt": "The robot's grid uses tiles 64 pixels wide. Name that `TILE_SIZE` and use it to turn a column number into a pixel position.",
  "signature": "func column_to_pixels(column):",
  "fn": "column_to_pixels",
  "starter": "func column_to_pixels(column):\n\treturn column * 64",
  "require_names": [
    "TILE_SIZE"
  ],
  "once_only": [
    64
  ],
  "tests": [
    {
      "name": "Column 0 is at 0",
      "args": [
        0
      ],
      "expect": 0
    },
    {
      "name": "Column 3 is at 192",
      "args": [
        3
      ],
      "expect": 192
    },
    {
      "name": "Column 10 is at 640",
      "args": [
        10
      ],
      "expect": 640
    }
  ],
  "hints": [
    "After naming it, the only place 64 should appear is the const line.",
    "`const TILE_SIZE = 64`"
  ],
  "docs": [
    {
      "name": "const NAME = value",
      "what": "Declares a named value that never changes. Written in CAPITALS by convention."
    }
  ],
  "solution": "func column_to_pixels(column):\n\tconst TILE_SIZE = 64\n\treturn column * TILE_SIZE"
}
