{
  "id": "gqld-001",
  "title": "Print every key",
  "concept": "gq-loopdicts",
  "difficulty": 0,
  "prompt": "The inventory is a dictionary of item names to amounts. Print each item name on its own line, in order.",
  "signature": "func print_keys(inventory):",
  "fn": "print_keys",
  "starter": "func print_keys(inventory):\n\tpass",
  "tests": [
    {
      "name": "Two names",
      "args": [
        {
          "bolt": 3,
          "gear": 1
        }
      ],
      "expect": null,
      "out": [
        "bolt",
        "gear"
      ]
    },
    {
      "name": "An empty dictionary prints nothing",
      "args": [
        {}
      ],
      "expect": null,
      "out": []
    }
  ],
  "hints": [
    "Looping over a dictionary gives you its keys.",
    "`for item in inventory:` then `print(item)`."
  ],
  "docs": [
    {
      "name": "for key in data:",
      "what": "Runs once per key; data[key] gives its value."
    },
    {
      "name": "print(value)",
      "what": "Shows value in the output. Takes anything; returns nothing."
    }
  ],
  "solution": "func print_keys(inventory):\n\tfor item in inventory:\n\t\tprint(item)"
}
