{
  "id": "gqfunc-006",
  "title": "Name it well",
  "concept": "gq-functions",
  "difficulty": 0,
  "prompt": "This robot code works, but `f` tells nobody what it does. Rename the function to `raise_shields`, both where it is defined and where solve calls it. The output stays \"Shields up\".",
  "signature": "func solve():",
  "starter": "func solve():\n\tf()\n\nfunc f():\n\tprint(\"Shields up\")",
  "require_methods": [
    "raise_shields"
  ],
  "tests": [
    {
      "name": "Still prints Shields up",
      "args": [],
      "expect": null,
      "out": [
        "Shields up"
      ]
    }
  ],
  "hints": [
    "A good function name says what the function does, so the code reads like a sentence.",
    "Change the name in two places: after func, and in the call inside solve."
  ],
  "docs": [
    {
      "name": "print(value)",
      "what": "Shows value in the output. Takes anything; returns nothing."
    }
  ],
  "solution": "func solve():\n\traise_shields()\n\nfunc raise_shields():\n\tprint(\"Shields up\")"
}
