{
  "id": "gqtype-011",
  "title": "void means nothing back",
  "concept": "gq-types",
  "difficulty": 0,
  "prompt": "Write `log_health`, hinted to return void, which prints the health. In `solve`, call it. Then return the health, unchanged.",
  "signature": "func solve(health: int) -> int:",
  "starter": "func solve(health: int) -> int:\n\treturn health",
  "tests": [
    {
      "name": "Health is printed and returned unchanged",
      "args": [
        80
      ],
      "expect": 80,
      "out": [
        "80"
      ]
    },
    {
      "name": "Zero health is printed and returned",
      "args": [
        0
      ],
      "expect": 0,
      "out": [
        "0"
      ]
    }
  ],
  "hints": [
    "-> void promises the function returns nothing.",
    "`func log_health(health: int) -> void:` with a print inside."
  ],
  "docs": [
    {
      "name": "func f() -> int:",
      "what": "Return type hint: the function promises to return that type."
    },
    {
      "name": "print(value)",
      "what": "Shows value in the output."
    }
  ],
  "solution": "func solve(health: int) -> int:\n\tlog_health(health)\n\treturn health\n\nfunc log_health(health: int) -> void:\n\tprint(health)",
  "allow_type_hints": true,
  "require_methods": [
    "log_health"
  ],
  "generator": [
    {
      "int": [
        0,
        120
      ]
    }
  ]
}
