{
  "id": "gqtype-004",
  "title": "Infer the type",
  "concept": "gq-types",
  "difficulty": 0,
  "prompt": "Declare `speed` using `:=` with the value 250, so Godot infers it is an int. Return speed times seconds as a float.",
  "signature": "func solve(seconds: float) -> float:",
  "starter": "func solve(seconds: float) -> float:\n\treturn 0.0",
  "tests": [
    {
      "name": "1 second is 250",
      "args": [
        1
      ],
      "expect": 250
    },
    {
      "name": "Half a second is 125",
      "args": [
        0.5
      ],
      "expect": 125
    }
  ],
  "hints": [
    "`:=` declares and infers the type in one go.",
    "`var speed := 250`, then `return speed * seconds`."
  ],
  "docs": [
    {
      "name": "var name := value",
      "what": "Infers the type from the value: `var x := 5` makes x an int."
    }
  ],
  "solution": "func solve(seconds: float) -> float:\n\tvar speed := 250\n\treturn speed * seconds",
  "allow_type_hints": true,
  "require_names": [
    "speed"
  ]
}
