{
  "id": "gqread-009",
  "title": "Name the win score",
  "concept": "gq-readable",
  "difficulty": 0,
  "prompt": "The robot wins at 1000 points. This returns how many points are still needed. Name the 1000 `WIN_SCORE`.",
  "signature": "func points_needed(score):",
  "fn": "points_needed",
  "starter": "func points_needed(score):\n\treturn 1000 - score",
  "require_names": [
    "WIN_SCORE"
  ],
  "once_only": [
    1000
  ],
  "tests": [
    {
      "name": "400 points needs 600 more",
      "args": [
        400
      ],
      "expect": 600
    },
    {
      "name": "0 points needs 1000",
      "args": [
        0
      ],
      "expect": 1000
    },
    {
      "name": "1000 points needs 0",
      "args": [
        1000
      ],
      "expect": 0
    }
  ],
  "hints": [
    "Same recipe: one const line, then use the name.",
    "`const WIN_SCORE = 1000`"
  ],
  "docs": [
    {
      "name": "const NAME = value",
      "what": "Declares a named value that never changes. Written in CAPITALS by convention."
    }
  ],
  "solution": "func points_needed(score):\n\tconst WIN_SCORE = 1000\n\treturn WIN_SCORE - score"
}
