{
  "id": "gqvar-001",
  "title": "Store and return a number",
  "concept": "gq-variables",
  "difficulty": 0,
  "prompt": "Create a variable called `lives` and store the number 3 in it. Then return lives.",
  "signature": "func solve() -> int:",
  "starter": "func solve() -> int:\n\treturn 0",
  "require_names": [
    "lives"
  ],
  "tests": [
    {
      "args": [],
      "expect": 3
    }
  ],
  "hint": "A variable needs three things: the word var, a name, and a value. Then return the name, not the number.",
  "solution": "func solve() -> int:\n\tvar lives = 3\n\treturn lives"
}
