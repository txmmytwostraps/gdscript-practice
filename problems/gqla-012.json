{
  "id": "gqla-012",
  "title": "Only the positives",
  "concept": "gq-looparrays",
  "difficulty": 1,
  "prompt": "Return a new array with only the numbers greater than zero, in their original order.",
  "signature": "func solve(nums):",
  "starter": "func solve(nums):\n\treturn nums",
  "tests": [
    {
      "name": "Keeps 1 and 3",
      "args": [
        [
          1,
          -2,
          3
        ]
      ],
      "expect": [
        1,
        3
      ]
    },
    {
      "name": "Nothing positive",
      "args": [
        [
          -1,
          0
        ]
      ],
      "expect": []
    },
    {
      "name": "All positive",
      "args": [
        [
          4,
          5
        ]
      ],
      "expect": [
        4,
        5
      ]
    }
  ],
  "hints": [
    "Loop, test, append.",
    "`if n > 0:` then `result.append(n)`."
  ],
  "docs": [
    {
      "name": "for item in list:",
      "what": "Runs the indented lines once per item, with item holding each value in turn."
    },
    {
      "name": "list.append(value)",
      "what": "Adds value to the end of the array. Returns nothing."
    }
  ],
  "solution": "func solve(nums):\n\tvar result = []\n\tfor n in nums:\n\t\tif n > 0:\n\t\t\tresult.append(n)\n\treturn result"
}
