{
  "id": "gqap-008",
  "title": "Is the queue empty",
  "concept": "gq-appendpop",
  "difficulty": 0,
  "prompt": "Return true if there are no orders waiting.",
  "signature": "func is_queue_empty(queue):",
  "fn": "is_queue_empty",
  "starter": "func is_queue_empty(queue):\n\treturn false",
  "tests": [
    {
      "name": "Empty is true",
      "args": [
        []
      ],
      "expect": true
    },
    {
      "name": "One order is false",
      "args": [
        [
          "soup"
        ]
      ],
      "expect": false
    }
  ],
  "hints": [
    "is_empty asks exactly this.",
    "`return queue.is_empty()`"
  ],
  "docs": [
    {
      "name": "list.is_empty()",
      "what": "Returns true when the array has no items."
    }
  ],
  "solution": "func is_queue_empty(queue):\n\treturn queue.is_empty()",
  "generator": [
    {
      "array": {
        "len": [
          0,
          3
        ],
        "of": {
          "pick": [
            "soup"
          ]
        }
      }
    }
  ]
}
