{
  "id": "vars-003",
  "title": "Add ten",
  "concept": "variables",
  "difficulty": 0,
  "prompt": "A variable `score` starts out equal to n. Add the number `10` to it, then return score.",
  "signature": "func solve(n: int) -> int:",
  "starter": "func solve(n: int) -> int:\n\tvar score = n\n\treturn score",
  "tests": [
    {
      "args": [
        0
      ],
      "expect": 10
    },
    {
      "args": [
        5
      ],
      "expect": 15
    },
    {
      "args": [
        -10
      ],
      "expect": 0
    },
    {
      "args": [
        100
      ],
      "expect": 110
    }
  ],
  "hint": "`score += 10` is a short way of writing `score = score + 10`.",
  "solution": "func solve(n: int) -> int:\n\tvar score = n\n\tscore += 10\n\treturn score"
}
