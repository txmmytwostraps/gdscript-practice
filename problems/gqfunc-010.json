{
  "id": "gqfunc-010",
  "title": "Two helpers, one job each",
  "concept": "gq-functions",
  "difficulty": 0,
  "prompt": "The robot introduces itself in two parts. Write `say_name`, which prints \"Robo\", and `say_model`, which prints \"RX-7\". Call both from `solve`, name first.",
  "signature": "func solve():",
  "starter": "func solve():\n\tpass",
  "require_methods": [
    "say_name",
    "say_model"
  ],
  "tests": [
    {
      "name": "Name, then model",
      "args": [],
      "expect": null,
      "out": [
        "Robo",
        "RX-7"
      ]
    }
  ],
  "hints": [
    "Each helper does one small thing; solve just lines them up.",
    "Two definitions with one print each, then two calls in solve."
  ],
  "docs": [
    {
      "name": "print(value)",
      "what": "Shows value in the output. Takes anything; returns nothing."
    }
  ],
  "solution": "func solve():\n\tsay_name()\n\tsay_model()\n\nfunc say_name():\n\tprint(\"Robo\")\n\nfunc say_model():\n\tprint(\"RX-7\")"
}
