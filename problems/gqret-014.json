{
  "id": "gqret-014",
  "title": "A helper that returns nothing",
  "concept": "gq-return",
  "difficulty": 0,
  "prompt": "Write `log_it`, which prints its parameter and returns nothing. In solve, call log_it(n), then return n. Notice the difference: the helper prints, solve returns.",
  "signature": "func solve(n: int) -> int:",
  "starter": "func solve(n: int) -> int:\n\treturn n",
  "require_methods": [
    "log_it"
  ],
  "tests": [
    {
      "args": [
        5
      ],
      "expect": 5,
      "out": [
        "5"
      ]
    },
    {
      "args": [
        0
      ],
      "expect": 0,
      "out": [
        "0"
      ]
    }
  ],
  "hint": "A function with no return line (or -> void) simply finishes. Printing is not returning.",
  "solution": "func solve(n: int) -> int:\n\tlog_it(n)\n\treturn n\n\nfunc log_it(value: int) -> void:\n\tprint(value)"
}
