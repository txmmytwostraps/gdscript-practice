{
  "id": "gqloop-008",
  "title": "Stop at a wall",
  "concept": "gq-loop",
  "difficulty": 1,
  "prompt": "The robot drives at 200 per second from `x` at 0.0, but there is a wall at 150 it can never pass. After 60 frames at 60 per second, x_at_wall reports 150.",
  "signature": "func x_at_wall():",
  "fn": "x_at_wall",
  "starter": "var x = 0.0\n\nfunc x_at_wall():\n\treturn round(x)",
  "tests": [
    {
      "name": "After one second, stopped at 150",
      "args": [],
      "expect": 150,
      "frames": 60
    },
    {
      "name": "After a quarter second, 50",
      "args": [],
      "expect": 50,
      "frames": 15
    }
  ],
  "hints": [
    "In _process, move x by 200 times delta first, then if it went past 150, put it back to 150.",
    "`x += 200 * delta` then `if x > 150:` set `x = 150`."
  ],
  "docs": [
    {
      "name": "func _process(delta):",
      "what": "Godot calls this once per frame, about 60 times a second. delta is the frame time in seconds."
    },
    {
      "name": "var name = value (outside any function)",
      "what": "A member variable keeps its value from one frame to the next."
    },
    {
      "name": "round(x)",
      "what": "Returns the nearest whole number as a float."
    }
  ],
  "solution": "var x = 0.0\n\nfunc _process(delta):\n\tx += 200 * delta\n\tif x > 150:\n\t\tx = 150\n\nfunc x_at_wall():\n\treturn round(x)",
  "require_methods": [
    "_process"
  ]
}
