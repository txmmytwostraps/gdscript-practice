{
  "id": "arr-005",
  "title": "Middle item",
  "concept": "arrays",
  "difficulty": 1,
  "prompt": "The array always has an odd number of items. Return the one in the middle.",
  "signature": "func solve(items: Array) -> int:",
  "starter": "func solve(items: Array) -> int:\n\treturn items[0]",
  "tests": [
    {
      "args": [
        [
          1,
          2,
          3
        ]
      ],
      "expect": 2
    },
    {
      "args": [
        [
          5
        ]
      ],
      "expect": 5
    },
    {
      "args": [
        [
          4,
          8,
          15,
          16,
          23
        ]
      ],
      "expect": 15
    },
    {
      "args": [
        [
          9,
          7,
          5,
          3,
          1
        ]
      ],
      "expect": 5
    }
  ],
  "hint": "The middle position is `items.size() / 2` (whole-number division: 5 / 2 is 2, and position 2 is the third item because positions start at 0).",
  "solution": "func solve(items: Array) -> int:\n\tvar middle = items.size() / 2\n\treturn items[middle]"
}
