{
  "id": "gqread-013",
  "title": "Name the discount",
  "concept": "gq-readable",
  "difficulty": 0,
  "prompt": "Items are 25% off. Name the 0.25 `DISCOUNT` and use it to work out the sale price.",
  "signature": "func solve(price: float) -> float:",
  "starter": "func solve(price: float) -> float:\n\treturn price - price * 0.25",
  "require_names": [
    "DISCOUNT"
  ],
  "once_only": [
    0.25
  ],
  "tests": [
    {
      "args": [
        100
      ],
      "expect": 75
    },
    {
      "args": [
        0
      ],
      "expect": 0
    },
    {
      "args": [
        40
      ],
      "expect": 30
    }
  ],
  "hint": "25% as a decimal is the number to name.",
  "solution": "func solve(price: float) -> float:\n\tconst DISCOUNT = 0.25\n\treturn price - price * DISCOUNT"
}
