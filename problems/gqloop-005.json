{
  "id": "gqloop-005",
  "title": "Countdown in the loop",
  "concept": "gq-loop",
  "difficulty": 1,
  "prompt": "A member `time_left` starts at 3.0. In `_process`, subtract delta each frame. After 60 frames time_after_frames returns time_left rounded, which is 2.",
  "signature": "func time_after_frames():",
  "fn": "time_after_frames",
  "starter": "var time_left = 3.0\n\nfunc time_after_frames():\n\treturn round(time_left)",
  "tests": [
    {
      "name": "After 1 second, 2 left",
      "args": [],
      "expect": 2,
      "frames": 60
    },
    {
      "name": "After 3 seconds, 0 left",
      "args": [],
      "expect": 0,
      "frames": 180
    },
    {
      "name": "No frames, 3 left",
      "args": [],
      "expect": 3,
      "frames": 0
    }
  ],
  "hints": [
    "A timer is a member that shrinks by delta every frame.",
    "`time_left -= delta`"
  ],
  "docs": [
    {
      "name": "func _process(delta):",
      "what": "Godot calls this once per frame, about 60 times a second. delta is the frame time in seconds."
    },
    {
      "name": "var name = value (outside any function)",
      "what": "A member variable keeps its value from one frame to the next."
    },
    {
      "name": "round(x)",
      "what": "Returns the nearest whole number as a float."
    }
  ],
  "solution": "var time_left = 3.0\n\nfunc _process(delta):\n\ttime_left -= delta\n\nfunc time_after_frames():\n\treturn round(time_left)",
  "require_methods": [
    "_process"
  ]
}
