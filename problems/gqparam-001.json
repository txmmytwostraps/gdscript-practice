{
  "id": "gqparam-001",
  "title": "Display a value",
  "concept": "gq-parameters",
  "difficulty": 0,
  "prompt": "The robot needs a way to display any number. Write `display`, which takes one value and prints it. `solve` receives n; make it call display with n, then with n times 2, so solve(3) prints 3 and then 6.",
  "signature": "func solve(n):",
  "starter": "func solve(n):\n\tpass",
  "require_methods": [
    "display"
  ],
  "tests": [
    {
      "name": "Prints 3 then 6",
      "args": [
        3
      ],
      "expect": null,
      "out": [
        "3",
        "6"
      ]
    },
    {
      "name": "Prints 0 then 0",
      "args": [
        0
      ],
      "expect": null,
      "out": [
        "0",
        "0"
      ]
    },
    {
      "name": "Negative numbers work",
      "args": [
        -2
      ],
      "expect": null,
      "out": [
        "-2",
        "-4"
      ]
    }
  ],
  "hints": [
    "A parameter is a name in the brackets of the definition; whatever the caller passes in is stored under that name.",
    "`func display(value):` with `print(value)` inside.",
    "In solve: `display(n)` then `display(n * 2)`."
  ],
  "docs": [
    {
      "name": "print(value)",
      "what": "Shows value in the output. Takes anything; returns nothing."
    }
  ],
  "solution": "func solve(n):\n\tdisplay(n)\n\tdisplay(n * 2)\n\nfunc display(value):\n\tprint(value)"
}
