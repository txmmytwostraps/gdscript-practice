{
  "id": "gqparam-001",
  "title": "Show a value",
  "concept": "gq-parameters",
  "difficulty": 0,
  "prompt": "The robot needs a way to display any number. Write `show`, which takes one value and prints it. `solve` receives n; make it call show with n, then with n times 2, so solve(3) prints 3 and then 6.",
  "signature": "func solve(n):",
  "starter": "func solve(n):\n\tpass",
  "require_methods": [
    "show"
  ],
  "tests": [
    {
      "name": "Prints 3 then 6",
      "args": [
        3
      ],
      "expect": null,
      "out": [
        "3",
        "6"
      ]
    },
    {
      "name": "Prints 0 then 0",
      "args": [
        0
      ],
      "expect": null,
      "out": [
        "0",
        "0"
      ]
    },
    {
      "name": "Negative numbers work",
      "args": [
        -2
      ],
      "expect": null,
      "out": [
        "-2",
        "-4"
      ]
    }
  ],
  "hints": [
    "A parameter is a name in the brackets of the definition; whatever the caller passes in is stored under that name.",
    "`func show(value):` with `print(value)` inside.",
    "In solve: `show(n)` then `show(n * 2)`."
  ],
  "docs": [
    {
      "name": "print(value)",
      "what": "Shows value in the output. Takes anything; returns nothing."
    }
  ],
  "solution": "func solve(n):\n\tshow(n)\n\tshow(n * 2)\n\nfunc show(value):\n\tprint(value)"
}
