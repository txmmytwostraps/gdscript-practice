{
  "id": "gqret-004",
  "title": "Use a returned value",
  "concept": "gq-return",
  "difficulty": 0,
  "prompt": "Write `square`, which returns its parameter times itself. In `solve`, store square(n) in a variable, add the number `1` to it, and return the result. solve(3) is 10.",
  "signature": "func solve(n):",
  "starter": "func solve(n):\n\treturn n",
  "tests": [
    {
      "name": "3 squared plus 1 is 10",
      "args": [
        3
      ],
      "expect": 10
    },
    {
      "name": "0 squared plus 1 is 1",
      "args": [
        0
      ],
      "expect": 1
    },
    {
      "name": "-2 squared plus 1 is 5",
      "args": [
        -2
      ],
      "expect": 5
    }
  ],
  "hints": [
    "What a function returns can be caught in a variable.",
    "`var squared = square(n)` then `return squared + 1`."
  ],
  "docs": [
    {
      "name": "return value",
      "what": "Ends the function and hands value back to whoever called it."
    }
  ],
  "solution": "func solve(n):\n\tvar squared = square(n)\n\treturn squared + 1\n\nfunc square(value):\n\treturn value * value",
  "require_methods": [
    "square"
  ]
}
