{
  "id": "gqvar-015",
  "title": "Read a variable three times",
  "concept": "gq-variables",
  "difficulty": 0,
  "prompt": "The variable `price` holds the value p. Return price added to itself three times (price + price + price).",
  "signature": "func solve(p: float) -> float:",
  "starter": "func solve(p: float) -> float:\n\tvar price = p\n\treturn price",
  "tests": [
    {
      "args": [
        2
      ],
      "expect": 6
    },
    {
      "args": [
        0
      ],
      "expect": 0
    },
    {
      "args": [
        1.5
      ],
      "expect": 4.5
    }
  ],
  "hint": "Reading a variable does not change it, so you can use it as many times as you like.",
  "solution": "func solve(p: float) -> float:\n\tvar price = p\n\treturn price + price + price"
}
