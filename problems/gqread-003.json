{
  "id": "gqread-003",
  "title": "Name the tax rate",
  "concept": "gq-readable",
  "difficulty": 0,
  "prompt": "The 0.2 here is a 20% tax. Name it `TAX_RATE` so the next reader does not have to guess.",
  "signature": "func solve(price: float) -> float:",
  "starter": "func solve(price: float) -> float:\n\treturn price + price * 0.2",
  "require_names": [
    "TAX_RATE"
  ],
  "once_only": [
    0.2
  ],
  "tests": [
    {
      "args": [
        10
      ],
      "expect": 12
    },
    {
      "args": [
        0
      ],
      "expect": 0
    },
    {
      "args": [
        50
      ],
      "expect": 60
    }
  ],
  "hint": "Constants are usually written in CAPITALS with underscores. The value can be a decimal.",
  "solution": "func solve(price: float) -> float:\n\tconst TAX_RATE = 0.2\n\treturn price + price * TAX_RATE"
}
