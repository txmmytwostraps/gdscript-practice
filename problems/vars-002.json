{
  "id": "vars-002",
  "title": "Store and return",
  "concept": "variables",
  "difficulty": 0,
  "prompt": "Inside solve(), create a variable named `greeting` that holds the text \"hello\". Then return that variable.",
  "signature": "func solve() -> String:",
  "starter": "func solve() -> String:\n\treturn \"\"",
  "tests": [
    {
      "args": [],
      "expect": "hello"
    }
  ],
  "hint": "A variable is made with `var`: `var greeting = \"hello\"`. Then `return greeting`.",
  "solution": "func solve() -> String:\n\tvar greeting = \"hello\"\n\treturn greeting"
}
